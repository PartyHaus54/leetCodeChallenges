const tictactoe = require('./tictactoe.js');

describe.only('tictactoe', () => {
  it('should find a winner in row', () => {
    expect(tictactoe([[0, 0], [0, 1], [1, 0], [0, 2], [2, 0]])).toBe('A');
  });
  it('should find a winner in row', () => {
    expect(tictactoe([[0, 0], [0, 1], [1, 0], [0, 2], [2, 0]])).toBe('A');
  });
  it('should find a winner in row', () => {
    expect(tictactoe([[0, 0], [0, 1], [1, 0], [0, 2], [2, 0]])).toBe('A');
  });
  it('should find a winner in row', () => {
    expect(tictactoe([[0, 0], [0, 1], [1, 0], [0, 2], [2, 0]])).toBe('A');
  });
  it('should find a winner in row', () => {
    expect(tictactoe([[0, 0], [0, 1], [1, 0], [0, 2], [2, 0]])).toBe('A');
  });
});